<h1 class="text-xl">Nuestro Productos</h1>
<% if(products.length>0){ %>
<ul class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-2 mt-2">
  <% products.map((product) => { %> <%- include("../partials/product",{product})
  %> <%}) %>
</ul>
<%- pagination %> <% }else{ %>
<p>No hay productos :(</p>
<%} %>
