<form
  action="/login"
  method="POST"
  class="mx-auto mt-15 w-full max-w-[400px] border border-gray-400 rounded-md p-2 md:p-5 shadow-md bg-white"
>
  <h1 class="text-2xl font-bold">Iniciar sesión</h1>
  <div class="flex flex-col gap-2">
    <label for="email" class="flex flex-col">
      <span class="text-sm">Correo electrónico</span>
      <input
        type="email"
        id="email"
        name="email"
        class="border p-1 rounded"
        required
        value="<%= oldData?.email || '' %>"
      />
    </label>
    <label for="password" class="flex flex-col">
      <span class="text-sm">Contraseña</span>
      <input
        type="password"
        id="password"
        name="password"
        class="border p-1 rounded"
        required
      />
    </label>
  </div>
  <% if (errors) { %>
  <div class="bg-red-200 text-red-800 border border-red-700 p-1 mt-2">
    <%= errors %>
  </div>
  <% } %>
  <button
    type="submit"
    class="bg-blue-600 text-white p-2 rounded mt-3 transition-colors w-full md:w-fit hover:bg-blue-700 cursor-pointer"
  >
    Iniciar sesión
  </button>
</form>
