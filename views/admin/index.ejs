<h1 class="text-xl">Bienvenido <%= user.name %></h1>
<div class="flex gap-2 mt-3">
  <a
    href="/products/create"
    class="bg-white border border-blue-800 rounded text-blue-600 p-1"
    >Añadir producto</a
  >
</div>
<% if(products.length>0){ %>
<ul class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-2 mt-2">
  <% products.map((product) => { %> <%- include("../partials/product",{product})
  %> <%}) %>
</ul>
<%- pagination %> <% }else{ %>
<p>No hay productos :(</p>
<%} %>
